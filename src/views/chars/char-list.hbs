<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta {{name}}="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://kit.fontawesome.com/c4a8f670ae.js"></script>
    <title>WoW Characters</title>
</head>

<body>
    <header>
        <div class="header-container">
            <h1>Your characters</h1>
            <nav>
                <a href="/chars/new-char" class="nav__link">Add new character</a>
                <a href="/auth/login" class="nav__link">Login</a>
                <form action="/auth/logout" method="POST" class="nav__link">
                    <button type="submit" value="Logout">Logout</button>
                </form>
            </nav>
        </div>
    </header>
    <main class="char__container">
        {{#each chars}}
        <div class="char__card"
            style="background-image: linear-gradient(145deg, rgba(22, 16, 12, 1), rgba(22, 16, 12, 0.50)), url({{pic}})">
            <div class="char__up-group">
                <h2 class="char__name">{{name}}</h2>
                <div class="char__icons-group">
                    <a href="/chars/{{_id}}" class="char__icon"><i class="fas fa-eye"></i></a>
                    <a href="/chars/edit-char/{{_id}}" class="char__icon"><i class="fas fa-edit"></i></a>
                    <form action="/chars/{{_id}}?_method=DELETE" method="POST">
                        <button type="submit" class="char__icon"><i class="fas fa-trash-alt"></i></button>
                    </form>
                </div>
            </div>
            <div class="char__down-group">
                <div>Lv {{level}}</div>
                <div>{{race}}</div>
                <div>{{charClass}}</div>
            </div>
        </div>
        {{/each}}
    </main>
</body>

</html>