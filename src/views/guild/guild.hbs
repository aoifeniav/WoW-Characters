<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://kit.fontawesome.com/c4a8f670ae.js"></script>
    <title>WoW Characters</title>
</head>

<body>
    <header>
        <div class="header-container">
            <h1>{{guild.name}}</h1>
            <nav>
                <a href="/chars/" class="nav__link">Characters list</a>
                <a href="/chars/new-char" class="nav__link">Add new character</a>
                <form action="/auth/logout" method="POST" class="nav__link">
                    <button type="submit" value="Logout">Logout</button>
                </form>
            </nav>
        </div>
    </header>
    <main class="chars__container">
        {{#each guild.members}}
        <div class="chars__card" style="background-image: linear-gradient(105deg, rgba(22,16,12,1) 0%, rgba(22,16,12,1) 40%, rgba(22,16,12,0) 50%), url({{pic}});
                {{#ifEquals race 'gnome'}} background-position-y: 57% {{/ifEquals}}
                {{#ifEquals race 'goblin'}} background-position-y: 55% {{/ifEquals}}
                {{#ifEquals race 'mechagnome'}} background-position-y: 57% {{/ifEquals}}
                {{#ifEquals race 'vulpera'}} background-position-y: 56% {{/ifEquals}}">
            <div class="chars__up-group">
                <h2 class="chars__name">{{name}}</h2>
                <div class="chars__icons-group">
                    <a href="/chars/{{_id}}" class="chars__icon"><i class="fas fa-eye"></i></a>
                    <a href="/chars/edit-char/{{_id}}" class="chars__icon"><i class="fas fa-edit"></i></a>
                    <form action="/chars/{{_id}}?_method=DELETE" method="POST">
                        <button type="submit" class="chars__icon"><i class="fas fa-trash-alt"></i></button>
                    </form>
                </div>
            </div>
            <div class="chars__down-group">
                <div>Lv {{level}}</div>
                <div>{{race}}</div>
                <div>{{charClass}}</div>
            </div>
        </div>
        {{/each}}
    </main>
</body>

</html>